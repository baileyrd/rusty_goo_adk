[package]
name = "adk-features"
description = "Feature-flag registry, ported from google/adk-python's features/ module (Phase 12)"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
